These are the installation scripts for the several components of the server.

Install the libs for each component before itself. Check the bash files for server paths' - the referenced folders must exist.

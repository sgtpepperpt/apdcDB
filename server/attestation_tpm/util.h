#include <string>
#include <stdexcept>
#include <memory>
#include <string>
#include <vector>

std::vector<std::string> split(const std::string &s, char delim);
std::string read_command(std::string comm);
